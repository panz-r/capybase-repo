# Processed, usable test cases (realworld/*.json etc.). Never committed —
# generated by scripts/fetch_mergeconflict_datasets.py from external-datasets/.
# tests/realworld_loader.py reads from here; tests skip cleanly when empty.
# Ignore everything but this .gitignore so the empty dir is tracked.
*
!.gitignore
