# Raw downloaded dataset archives (325MB+). Never committed — regenerated by
# scripts/fetch_mergeconflict_datasets.py. Ignore everything in this dir but
# keep this .gitignore so the directory exists on a fresh clone.
*
!.gitignore
