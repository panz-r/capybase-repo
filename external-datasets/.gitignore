# Extracted raw dataset trees (A/O/B/M folders etc.). Never committed —
# regenerated by scripts/fetch_mergeconflict_datasets.py from downloads/.
# Ignore everything but this .gitignore so the empty dir is tracked.
*
!.gitignore
